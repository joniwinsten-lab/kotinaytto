# Paikallinen CLI: `supabase link --project-ref <REFERENCE_ID>` tallentaa linkin supabase/.temp/
# GitHub Actions käyttää SUPABASE_PROJECT_REF -salaisuutta (ei tämän tiedoston nimeä).
# Reference ID: Dashboard → Project Settings → General.

project_id = "kotinaytto"

[api]
enabled = true

[db]
major_version = 15

[functions.sync-rss]
verify_jwt = false

[functions.sync-weather]
verify_jwt = false

[functions.photo-ingest]
verify_jwt = false

[functions.photo-delete]
verify_jwt = false
